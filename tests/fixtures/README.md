# Fixtures

This directory contains benchmark inputs and expected outputs used by regression tests.

`benchmark-manifest.json` defines required fixture families. Add concrete PDF and golden
xlsx artifacts per family as the corpus grows.
